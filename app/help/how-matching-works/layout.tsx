import type { ReactNode } from "react";

export const metadata = {
  title: "How matching works · NxtStps",
};

export default function HowMatchingLayout({ children }: { children: ReactNode }) {
  return children;
}
