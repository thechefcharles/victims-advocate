import type { ReactNode } from "react";

export const metadata = {
  title: "Help · NxtStps",
  description: "Support, trust, and resources on NxtStps.",
};

export default function HelpLayout({ children }: { children: ReactNode }) {
  return children;
}
