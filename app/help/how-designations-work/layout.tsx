import type { ReactNode } from "react";

export const metadata = {
  title: "How designations work · NxtStps",
};

export default function HowDesignationsLayout({ children }: { children: ReactNode }) {
  return children;
}
